package com.fuze.features.matches.data.database.mapper

import com.fuze.features.matches.data.database.entity.MatchEntity
import com.fuze.features.matches.data.remote.model.dto.MatchDto

/*internal fun List<MatchEntity>.toDto() = this.map {
    MatchDto(
        id = it.id, teams = it.firstTeamId, id = it.id, username = it.username
    )
}

internal fun List<MatchDto>.toEntity() = this.map {
    MatchEntity(
        imgUrl = it.img, name = it.name, id = it.id, username = it.username
    )
}*/
